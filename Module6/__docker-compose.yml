version: '3.1'
services:
  n8n:
    build: .
    container_name: n8n
    ports:
      - "5678:5678"
    env_file:
      - .env
    environment:
      - N8N_BASIC_AUTH_ACTIVE=true
      - N8N_BASIC_AUTH_KEY=epam_train_key 
      - N8N_RUNNERS_ENABLED=true
    volumes:
    - ~/.n8n:/home/node/.n8n